defmodule Bylaw.Ecto.Query do
  @moduledoc """
  Documentation namespace for checks that validate prepared Ecto queries.

  `Bylaw.Ecto.Query` is Bylaw's current public check family. Its checks inspect
  `Ecto.Query` structs before a repo operation runs.

  See the [`Bylaw.Ecto.Query` checks guide](ecto_query_checks.html) for
  `prepare_query/3` wiring, built-in checks, option keys, escape hatches, and
  issue metadata.
  """
end
