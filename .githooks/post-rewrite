#!/usr/bin/env bash
set -euo pipefail

command="${1:-}"

if [ "$command" != "rebase" ]; then
  exit 0
fi

hook_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$hook_dir/run-mix-qa" post-rewrite
