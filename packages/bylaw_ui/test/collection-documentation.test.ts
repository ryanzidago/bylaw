import { test } from "bun:test";

test("the README documents explicit collection targeting separately from singular targeting", () => {});

test("the README documents that an empty collection reports resolution failure and skips geometry", () => {});

test("the README shows representative containment equal-width vertical-ordering and non-overlap collection rules", () => {});

test("the README documents how target order determines collection evaluation and reporting order", () => {});

test("the README states that duplicate singular targets remain errors", () => {});
