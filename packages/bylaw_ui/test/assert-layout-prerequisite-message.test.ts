import { test } from "bun:test";

test("reports an invalid rule field and reason", () => {});

test("reports a missing element and its operand role", () => {});

test("reports duplicate element matches and their operand role", () => {});

test("reports a hidden element and its measured dimensions", () => {});

test("reports a zero-size element and its measured dimensions", () => {});
