import { test } from "bun:test";

test("an individual collection assertion diagnostic includes its index and target element", () => {});

test("a pairwise collection assertion diagnostic includes both indexes and target elements", () => {});

test("collection assertion diagnostics include measured values and expected constraints", () => {});

test("an empty collection assertion diagnostic is distinct from a missing singular target", () => {});

test("several findings from one collection rule do not inflate the assertion summary", () => {});

test("collection assertion diagnostics preserve finding order and the original report", () => {});
