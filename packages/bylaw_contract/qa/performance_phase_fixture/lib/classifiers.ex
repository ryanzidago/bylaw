defmodule BylawPhaseFixture.Classifier1 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier2 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier3 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier4 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier5 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier6 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier7 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier8 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier9 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier10 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier11 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end

defmodule BylawPhaseFixture.Classifier12 do
  @moduledoc false
  @spec classify(integer()) :: :positive | :nonpositive
  def classify(value) when value > 0, do: :positive
  def classify(_value), do: :nonpositive

  def choose(:left), do: :left
  def choose(:right), do: :right
end
